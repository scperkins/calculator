This is an ongoing project that will hopefully become more complex with added features.
Currently some of the functions such as the 'addition' function require the user to put spaces between the operands that they enter. 
